tasks:
  - name: Start Elastic stack in background
    command: |
      docker compose up
      gp sync-done stack
  - name: Configure python
    init: |
      export PIP_USER=false
      pip install --upgrade pip
      pyenv virtualenv lab
      sed -i "s/include-system-site-packages = false/include-system-site-packages = true/" /home/gitpod/.pyenv/versions/lab/pyvenv.cfg;
      pyenv activate lab
      pip install -r requirements.txt
      python3 -m ipython kernel install --user --name=lab

# ports:
#   - port: 9200
#     onOpen: ignore
#     visibility: private

vscode:
  extensions:
    - eamodio.gitlens
    - ria.elastic
    - ms-python.python
    - ms-python.anaconda-extension-pack
    - ms-azuretools.vscode-docker
    - ms-toolsai.jupyter
    - ms-toolsai.jupyter-keymap
    - ms-toolsai.jupyter-renderers
